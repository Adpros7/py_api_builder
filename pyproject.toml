[project]
name = "py-api-builder"
version = "0.1.0"
description = "A Python code generator that creates Flask API wrappers around external REST APIs"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
